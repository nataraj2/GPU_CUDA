name: Run Code

on:
  push:
    branches:
      - main

jobs:
  run_code:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Install Dependencies
      run: |
        npm install

    - name: Run Code
      run: |
        cd ./CPUAndGPU
        make
		./parallel_for_cpu.ex
